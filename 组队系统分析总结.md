# 组队系统分析总结

## 一、组队系统的职责

组队系统主要解决五件事：

1. 管理队伍的创建和成员列表，支持最多 5 人组队。
2. 处理组队邀请请求，支持通过好友列表邀请玩家加入队伍。
3. 处理队伍离开请求，队员离开或队长离开时自动转让队长。
4. 同步队伍信息变化，队伍成员增减时通知所有队员。
5. 提供队伍界面，显示队员信息、队长标识、离开队伍等操作。


## 二、整体实现方法

客户端与服务端的分工：

| 服务端 `TeamManager` ： 单例管理器，管理所有队伍，维护角色与队伍的映射关系 |
| 服务端 `Team` ： 队伍模型，包含队伍 ID、队长、成员列表、时间戳 |
| 服务端 `TeamService` ： 处理组队邀请和离开请求，转发消息，验证状态 |
| 客户端 `TeamManager` ： 管理当前玩家的队伍信息，控制队伍 UI 显示 |
| 客户端 `TeamService` ： 发送组队请求，接收组队消息，处理响应 |
| `NTeamInfo` ： Protobuf 协议消息，传输队伍信息（Id、Leader、Members） |
| `UITeam` ： 队伍界面，显示队伍成员列表、队长标识、离开按钮 |
| `UITeamItem` ： 队伍成员列表项，显示成员姓名、等级、职业图标、队长图标 |

组队系统与好友系统协作，支持通过好友列表邀请好友组队。

## 三、队伍数据的定义

服务端 `Team` 模型定义队伍：

| `Id` ： 队伍 ID（在 Teams 列表中的索引） |
| `leader` ： 队长（Character 引用） |
| `Members` ： 队员列表（List<Character>） |
| `timestamp` ： 队伍最后更新时间戳 |

协议消息 `NTeamInfo` 传输队伍信息：

| `Id` ： 队伍 ID |
| `Leader` ： 队长角色 ID |
| `Members` ： 队员列表（List<NCharacterInfo>） |

### 队伍成员上限

当前队伍最多支持 5 人，在 `UITeam` 中硬编码：

```csharp
this.teamTitle.text = string.Format("我的队伍 ({0}/5)", User.Instance.TeamInfo.Members.Count);
```

## 四、队伍的创建和复用机制

服务端 `TeamManager` 采用 队伍复用 机制，减少对象创建开销：

```csharp
private Team CreateTeam(Character leader)
{
    Team team = null;
    // 1. 查找空闲队伍（成员数为 0）
    for(int i = 0; i < this.Teams.Count; i++)
    {
        team = this.Teams[i];
        if(team.Members.Count == 0)
        {
            // 复用空闲队伍
            team.AddMember(leader);
            return team;
        }
    }
    
    // 2. 没有空闲队伍，创建新队伍
    team = new Team(leader);
    this.Teams.Add(team);
    team.Id = this.Teams.Count;
    return team;
}
```

关键特性：

1. 队伍创建后永久保存在 `Teams` 列表中。
2. 当队伍所有成员都离开后（`Members.Count == 0`），下次创建队伍时复用这个队伍。
3. 队伍 ID 等于在 `Teams` 列表中的索引位置。

## 五、组队邀请的流程

--- 组队邀请的完整流程
    玩家 A 在好友列表中选择在线好友 B
    点击组队邀请按钮
    
    客户端 TeamService.SendTeamInviteRequest(friendId, friendName)
    发送 TeamInviteRequest 消息
        FromId = A 的 ID
        FromName = A 的姓名
        ToId = B 的 ID
        ToName = B 的姓名
    
    服务端 TeamService.OnTeamInviteRequest 接收请求
    验证：
        检查 B 是否在线（通过 SessionManager）
        检查 B 是否已有队伍（target.Session.Character.Team != null）
    将请求转发给玩家 B（target.Session.Response.teamInviteReq = request）
    
    玩家 B 收到 TeamInviteRequest 消息
    客户端 TeamService.OnTeamInviteRequest 显示确认对话框
    玩家 B 选择接受或拒绝
    
    客户端 TeamService.SendTeamInviteResponse(accept, request)
    发送 TeamInviteResponse 消息（通过 Response 通道）
    
    服务端 TeamService.OnTeamInviteResponse 接收响应
    如果接受（Result.Success）：
        获取请求者 A 的 Session
        调用 TeamManager.Instance.AddTeamMember(A, B)
            如果 A 没有队伍，创建队伍并将 A 加入
            将 B 加入 A 的队伍
        分别发送成功消息给 A 和 B
    如果拒绝：
        发送失败消息给 B
    
    队伍信息通过后处理机制同步
    客户端接收 TeamInfoResponse 更新队伍界面


### 服务端组队邀请的核心代码

```csharp
private void OnTeamInviteResponse(NetConnection<NetSession> sender, TeamInviteResponse response)
{
    Character character = sender.Session.Character;
    sender.Session.Response.teamInviteRes = response;
    
    if(response.Result == Result.Success)
    {
        var requester = SessionManager.Instance.GetSession(response.Request.FromId);
        if(requester == null)
        {
            sender.Session.Response.teamInviteRes.Result = Result.Failed;
            sender.Session.Response.teamInviteRes.Errormsg = "请求者已下线";
        }
        else
        {
            // 将 B 加入 A 的队伍
            TeamManager.Instance.AddTeamMember(requester.Session.Character, character);
            
            // 发送成功消息给请求者 A
            requester.Session.Response.teamInviteRes = response;
            requester.SendResponse();
        }
    }
    sender.SendResponse();
}
```

### 添加队员的逻辑

```csharp
internal void AddTeamMember(Character leader, Character member)
{
    if(leader.Team == null)
    {
        // 队长没有队伍，创建新队伍
        leader.Team = CreateTeam(leader);
    }
    // 将成员加入队长的队伍
    leader.Team.AddMember(member);
}
```

`Team.AddMember(member)` 的实现：

```csharp
public void AddMember(Character member)
{
    if(this.Members.Count == 0)
    {
        // 第一个成员自动成为队长
        this.leader = member;
    }
    this.Members.Add(member);
    member.Team = this; // 双向引用
    this.timestamp = TimeUtil.timestamp; // 更新时间戳
}
```

## 六、离开队伍的流程

--- 离开队伍的完整流程
    玩家点击离开队伍按钮
    显示确认对话框
    
    客户端 TeamService.SendTeamLeaveRequest(teamId)
    发送 TeamLeaveRequest 消息
        TeamId = 队伍 ID
        characterId = 角色 ID
    
    服务端 TeamService.OnTeamLeave 接收请求
    调用 character.Team.Leave(character)
        从队伍成员列表中移除角色
        如果离开的是队长：
            如果还有成员，第一个成员成为新队长
            如果没有成员，队长设为 null
        清除角色的 Team 引用
        更新队伍时间戳
    返回 TeamLeaveResponse
    
    队伍信息通过后处理机制同步给剩余队员
    客户端接收 TeamLeaveResponse
    更新本地队伍信息为 null
    隐藏队伍 UI


### 服务端离开队伍的核心代码

```csharp
private void OnTeamLeave(NetConnection<NetSession> sender, TeamLeaveRequest message)
{
    Character character = sender.Session.Character;
    sender.Session.Response.teamLeave = new TeamLeaveResponse();
    sender.Session.Response.teamLeave.characterId = message.characterId;
    sender.Session.Response.teamLeave.Result = Result.Success;

    // 离开队伍
    character.Team.Leave(character);

    sender.SendResponse();
}
```

`Team.Leave(member)` 的实现：

```csharp
public void Leave(Character member)
{
    Log.InfoFormat("Leave Team: {0}:{1}", member.Id, member.Name);
    
    // 1. 从成员列表中移除
    this.Members.Remove(member);
    
    // 2. 队长离开时的处理
    if(member == this.leader)
    {
        if(this.Members.Count > 0)
            this.leader = this.Members[0]; // 第一个成员成为新队长
        else 
            this.leader = null; // 队伍为空
    }
    
    // 3. 清除角色的队伍引用
    member.Team = null;
    
    // 4. 更新时间戳
    timestamp = TimeUtil.timestamp;
}
```

关键特性：

1. 队长离开时，第一个成员自动成为新队长。
2. 同时清除队伍的成员列表和角色的 Team 引用。
3. 当最后一个成员离开后，队伍变为空闲状态，可被复用。

## 七、队伍信息的后处理机制

服务端 `Team.PostProcess` 实现队伍信息的自动同步：

```csharp
public void PostProcess(NetMessageResponse message)
{
    if(message.teamInfo == null)
    {
        message.teamInfo = new TeamInfoResponse();
        message.teamInfo.Result = Result.Success;
        message.teamInfo.Team = new NTeamInfo();
        message.teamInfo.Team.Id = this.Id;
        message.teamInfo.Team.Leader = this.leader.Id;
        
        // 收集所有队员的基本信息
        foreach(var member in this.Members)
        {
            message.teamInfo.Team.Members.Add(member.GetBasicInfo());
        }
    }
}
```

后处理机制的触发：

1. 添加成员或成员离开时，`timestamp` 更新。
2. 在 `NetSession.GetResponse()` 中调用 `Character.PostProcess()`。
3. 如果角色有队伍，调用 `character.Team.PostProcess(response)`。
4. 队伍信息附加到响应消息中，发送给所有队员。

客户端接收 `TeamInfoResponse` 后更新队伍信息：

```csharp
private void OnTeamInfo(object sender, TeamInfoResponse message)
{
    Debug.Log("OnTeamInfo");
    TeamManager.Instance.UpdateTeamInfo(message.Team);
}
```

`TeamManager.UpdateTeamInfo` 的实现：

```csharp
public void UpdateTeamInfo(NTeamInfo team)
{
    User.Instance.TeamInfo = team;
    ShowTeamUI(team != null); // team 为 null 时隐藏队伍 UI
}
```

## 八、队伍界面的实现

`UITeam` 提供队伍信息显示和选择功能：

界面功能

| 队伍标题 ： 显示队伍人数（例如："我的队伍 (3/5)"） |
| 队员列表 ： 显示所有队员，包括姓名、等级、职业图标、队长标识 |
| 离开队伍按钮 ： 点击后显示确认对话框，确认后离开队伍 |

队伍 UI 的更新

```csharp
private void UpdateTeamUI()
{
    if(User.Instance.TeamInfo == null) return;
    
    // 更新队伍标题
    this.teamTitle.text = string.Format("我的队伍 ({0}/5)", User.Instance.TeamInfo.Members.Count);

    // 更新队员列表
    for(int i = 0; i < 5; i++)
    {
        if(i < User.Instance.TeamInfo.Members.Count)
        {
            // 显示队员信息
            this.Members[i].SetMemberInfo(
                i, 
                User.Instance.TeamInfo.Members[i], 
                User.Instance.TeamInfo.Members[i].Id == User.Instance.TeamInfo.Leader // 是否为队长
            );
            this.Members[i].gameObject.SetActive(true);
        }
        else
        {
            // 隐藏空位
            this.Members[i].gameObject.SetActive(false);
        }
    }
}
```

离开队伍的确认对话框

```csharp
public void OnClickLeave()
{
    MessageBox.Show("确定要离开队伍吗?", "退出队伍", MessageBoxType.Confirm, "确认", "取消").OnYes = () =>
    {
        TeamService.Instance.SendTeamLeaveRequest(User.Instance.TeamInfo.Id);
    };
}
```

## 九、队伍成员的显示

`UITeamItem` 显示单个队员的信息：

```csharp
public void SetMemberInfo(int idx, NCharacterInfo item, bool isLeader)
{
    this.idx = idx;
    this.Info = item;
    
    // 显示等级和姓名
    if(this.nickname != null) 
        this.nickname.text = this.Info.Level.ToString() + " " + this.Info.Name;
    
    // 显示职业图标
    if (this.classIcon != null) 
        this.classIcon.overrideSprite = SpriteManager.Instance.classIcons[(int)this.Info.Class - 1];
    
    // 显示队长标识
    if (this.leaderIcon != null) 
        this.leaderIcon.gameObject.SetActive(isLeader);
}
```

界面元素：

| `nickname` ： 显示"等级 姓名"（例如："25 张三"） |
| `classIcon` ： 显示职业图标 |
| `leaderIcon` ： 队长专属图标，只有队长显示 |
| `background` ： 选中时显示不同背景 |


## 十、相关代码

1. `Server/GameServer/GameServer/Managers/TeamManager.cs`
2. `Server/GameServer/GameServer/Models/Team.cs`
3. `Server/GameServer/GameServer/Services/TeamService.cs`
4. `Client/Assets/Scripts/Managers/TeamManager.cs`
5. `Client/Assets/Scripts/Services/TeamService.cs`
6. `Client/Assets/Scripts/UI/Team/UITeam.cs`
7. `Client/Assets/Scripts/UI/Team/UITeamItem.cs`
8. `Client/Assets/Scripts/UI/Friend/UIFriends.cs`
9. `Server/GameServer/GameServer/Entities/Character.cs`
10. `Lib/Protocol/message.cs` (NTeamInfo, TeamInviteRequest/Response, TeamLeaveRequest/Response)
11. `Server/GameServer/GameServer/Managers/SessionManager.cs`
12. `Common/Utils/TimeUtil.cs`
