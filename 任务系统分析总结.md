# 任务系统分析总结

## 一、任务系统的职责

任务系统主要解决五件事：

1. 定义任务的静态信息，包括任务类型、目标、对话文本、奖励等。
2. 记录玩家接受的任务状态（未接受、进行中、已完成、已提交）和目标进度。
3. 处理任务的接受和提交逻辑，验证前置任务、等级、职业限制。
4. 管理 NPC 与任务的关系，在 NPC 头上显示任务状态标记。
5. 发放任务奖励（金币、经验、道具），通过道具系统和金币系统实现。


## 二、整体实现方法

客户端与服务端的分工：

| `QuestDefine` ： 任务的静态配置，包含任务 ID、名称、类型、目标、NPC、对话、奖励等 |
| `TCharacterQuest` ： 数据库实体，保存玩家任务的 QuestID、状态、目标进度 |
| 服务端 `QuestManager` ： 每个角色一份，处理任务接受和提交，验证条件，发放奖励 |
| 客户端 `QuestManager` ： 管理任务列表，检查可用任务，维护 NPC 与任务的映射关系 |
| `QuestService` ： 服务端接收任务接受和提交请求，客户端发送请求和处理响应 |
| `Quest` ： 客户端任务模型，包含 QuestDefine（配置）和 NQuestInfo（状态） |
| `UIQuestDialog` ： 任务对话界面，显示任务信息，提供接受和提交按钮 |
| `NpcQuestStatus` ： NPC 头上的任务状态标记（可接受、进行中、已完成、无任务） |

任务系统与道具系统、金币系统紧密协作，奖励发放依赖这些系统的统一通知机制（依旧是状态后处理机制）。

## 三、任务数据的定义

`Common.Data.QuestDefine` 定义任务系统的配置，在 Excel 中可以配置这些信息：

### 基本信息

| `ID` ： 任务 ID |
| `Name` ： 任务名称 |
| `Type` ： 任务类型（Main 主线 / Branch 支线） |
| `LimitLevel` ： 等级限制 |
| `LimitClass` ： 职业限制 |
| `PreQuest` ： 前置任务 ID |

### NPC 和对话

| `AcceptNPC` ： 接受任务的 NPC ID |
| `SubmitNPC` ： 提交任务的 NPC ID |
| `Overview` ： 任务概述 |
| `Dialog` ： 任务对话 |
| `DialogAccept` ： 接受任务后的对话 |
| `DialogDeny` ： 拒绝任务的对话 |
| `DialogIncomplete` ： 任务未完成的对话 |
| `DialogFinish` ： 任务完成后的对话 |

### 任务目标（支持 3 个目标）

| `Target1`、`Target2`、`Target3` ： 目标类型（None / Kill 击杀 / Item 收集） |
| `Target1ID`、`Target2ID`、`Target3ID` ： 目标对象 ID（怪物 ID 或道具 ID） |
| `Target1Num`、`Target2Num`、`Target3Num` ： 目标数量 |

### 任务奖励

| `RewardGold` ： 奖励金币 |
| `RewardExp` ： 奖励经验 |
| `RewardItem1`、`RewardItem1Count` ： 奖励道具 1 和数量 |
| `RewardItem2`、`RewardItem2Count` ： 奖励道具 2 和数量 |
| `RewardItem3`、`RewardItem3Count` ： 奖励道具 3 和数量 |

数据库实体 `TCharacterQuest` 保存玩家任务状态：

| `Id` ： 数据库记录编号（任务实例的唯一标识） |
| `QuestID` ： 任务配置 ID（关联 QuestDefine） |
| `Target1`、`Target2`、`Target3` ： 各目标的当前进度 |
| `Status` ： 任务状态（InProgress 进行中 / Complated 已完成 / Finished 已提交） |
| `TCharacterID` ： 所属角色 ID |

## 四、进入游戏时的任务初始化

--- 玩家进入游戏时的服务端流程
    Character 构造函数创建角色
    创建 QuestManager 实例
    调用 QuestManager.GetQuestInfos(Info.Quests)
    遍历 character.Data.Quests（数据库任务列表）
    将 TCharacterQuest 转换为 NQuestInfo 协议消息
    NQuestInfo 包含 QuestId、QuestGuid、Status、Targets
    写入 NCharacterInfo.Quests
    返回 UserGameEnterResponse


--- 玩家进入游戏时的客户端流程
    客户端收到进入游戏响应
    调用 QuestManager.Init(response.Character.Quests)
    清空旧任务数据
    初始化已接受的任务（创建 Quest 对象）
    调用 CheckAvailableQuests() 检查可接受的任务
    为每个任务关联接受 NPC 和提交 NPC
    构建 npcQuests 映射表（NPC ID → 任务状态 → 任务列表）


## 五、可接受任务的检查

客户端 `CheckAvailableQuests()` 检查哪些任务可以接受：

```csharp
private void CheckAvailableQuests()
{
    foreach (var kv in DataManager.Instance.Quests)
    {
        // 1. 职业限制检查
        if (kv.Value.LimitClass != CharacterClass.None && kv.Value.LimitClass != User.Instance.CurrentCharacter.Class)
            continue;
        
        // 2. 等级限制检查
        if (kv.Value.LimitLevel > User.Instance.CurrentCharacter.Level)
            continue;
        
        // 3. 任务是否已存在
        if (this.allQuests.ContainsKey(kv.Key))
            continue;

        // 4. 前置任务检查
        if (kv.Value.PreQuest > 0)
        {
            Quest preQuest;
            if (this.allQuests.TryGetValue(kv.Value.PreQuest, out preQuest))
            {
                if (preQuest.info == null)
                    continue; // 前置任务未接取
                if (preQuest.info.Status != QuestStatus.Finished)
                    continue; // 前置任务未完成
            }
            else
                continue; // 前置任务不存在
        }
        
        // 通过检查，创建可接受任务
        Quest quest = new Quest(kv.Value);
        this.allQuests[quest.Define.ID] = quest;
    }
}
```

检查流程确保玩家只能看到符合条件的任务。

## 六、NPC 与任务的映射关系

客户端维护 `npcQuests` 映射表：

```csharp
Dictionary<int, Dictionary<NpcQuestStatus, List<Quest>>> npcQuests
```

结构：NPC ID → 任务状态 → 任务列表

任务状态类型：

| `NpcQuestStatus.None` ： 无任务 |
| `NpcQuestStatus.Available` ： 可接受任务 |
| `NpcQuestStatus.Incomplete` ： 进行中未完成 |
| `NpcQuestStatus.Complete` ： 已完成可提交 |

`AddNpcQuest(npcId, quest)` 将任务添加到对应 NPC 的列表：

- 如果 `quest.info == null`（未接取），添加到接受 NPC 的 Available 列表。
- 如果 `quest.info.Status == Complated`（已完成），添加到提交 NPC 的 Complete 列表。
- 如果 `quest.info.Status == InProgress`（进行中），添加到提交 NPC 的 Incomplete 列表。

`GetQuestStatusByNpc(npcId)` 返回 NPC 头上应显示的任务状态标记：

1. 优先返回 Complete（有已完成任务）。
2. 其次返回 Available（有可接受任务）。
3. 最后返回 Incomplete（有进行中任务）。
4. 都没有返回 None。

## 七、接受任务的流程

--- 接受任务的完整流程
    玩家与 NPC 交互
    QuestManager.OpenNpcQuest(npcId)
    根据任务状态显示 UIQuestDialog
    玩家点击接受按钮（WindowResult.Yes）
    
    客户端 QuestService.SendQuestAccept(quest)
    发送 QuestAcceptRequest 消息（包含 QuestId）
    
    服务端 QuestService.OnQuestAccept 接收请求
    调用 character.QuestManager.AcceptQuest(sender, questId)
    验证任务是否存在
    创建 TCharacterQuest 数据库实体
    设置 QuestID 和初始状态
        如果 Target1 == None：直接设为 Complated
        否则设为 InProgress
    添加到 character.Data.Quests
    保存数据库 DBService.Instance.Save()
    返回 QuestAcceptResponse（包含 NQuestInfo）
    
    客户端 QuestService.OnQuestAccept 接收响应
    调用 QuestManager.OnQuestAccepted(info)
    更新任务状态，刷新 npcQuests 映射
    显示接受任务后的对话 DialogAccept


服务端 `AcceptQuest` 的核心代码：

```csharp
public Result AcceptQuest(NetConnection<NetSession> sender, int questId)
{
    Character character = sender.Session.Character;
    QuestDefine quest;
    if(DataManager.Instance.Quests.TryGetValue(questId, out quest))
    {
        var dbquest = DBService.Instance.Entities.CharacterQuests.Create();
        dbquest.QuestID = quest.ID;
        
        // 判断任务初始状态
        if(quest.Target1 == QuestTarget.None)
        {
            // 没有目标直接完成
            dbquest.Status = (int)QuestStatus.Complated;
        }
        else
        {
            // 有目标的
            dbquest.Status = (int)QuestStatus.InProgress;
        }
        
        sender.Session.Response.questAccept.Quest = this.GetQuestInfo(dbquest);
        character.Data.Quests.Add(dbquest);
        DBService.Instance.Save();
        return Result.Success;
    }
    else
    {
        sender.Session.Response.questAccept.Errormsg = "任务不存在";
        return Result.Failed;
    }
}
```

## 八、提交任务的流程

--- 提交任务的完整流程
    玩家任务完成后与提交 NPC 交互
    QuestManager.OpenNpcQuest(npcId)
    显示 UIQuestDialog，显示提交按钮
    玩家点击提交按钮
    
    客户端 QuestService.SendQuestSubmit(quest)
    发送 QuestSubmitRequest 消息（包含 QuestId）
    
    服务端 QuestService.OnQuestSubmit 接收请求
    调用 character.QuestManager.SubmitRequest(sender, questId)
    查找玩家的任务记录
    验证任务状态是否为 Complated
    更新状态为 Finished
    发放奖励：
        金币：character.Gold += quest.RewardGold
        经验：character.Exp += quest.RewardExp（待实现）
        道具：character.ItemManager.AddItem(itemId, count)
    保存数据库 DBService.Instance.Save()
    返回 QuestSubmitResponse（包含 NQuestInfo）
    
    客户端 QuestService.OnQuestSubmit 接收响应
    调用 QuestManager.OnQuestSubmited(info)
    更新任务状态，刷新 npcQuests 映射
    检查新的可接受任务（前置任务解锁）
    显示完成任务后的对话 DialogFinish


服务端 `SubmitRequest` 的核心代码：

```csharp
public Result SubmitRequest(NetConnection<NetSession> sender, int questId)
{
    Character character = sender.Session.Character;
    QuestDefine quest;
    
    if(DataManager.Instance.Quests.TryGetValue(questId, out quest))
    {
        var dbquest = character.Data.Quests.Where(q => q.QuestID == questId).FirstOrDefault();
        if(dbquest != null)
        {
            // 验证任务状态
            if(dbquest.Status != (int)QuestStatus.Complated)
            {
                sender.Session.Response.questSubmit.Errormsg = "任务未完成";
                return Result.Failed;
            }
            
            // 更新状态
            dbquest.Status = (int)QuestStatus.Finished;
            sender.Session.Response.questSubmit.Quest = this.GetQuestInfo(dbquest);
            DBService.Instance.Save();

            // 发放奖励
            if(quest.RewardGold > 0)
                character.Gold += quest.RewardGold;
            
            if(quest.RewardExp > 0)
                ; // character.Exp += quest.RewardExp; 待实现
            
            if(quest.RewardItem1 > 0)
                character.ItemManager.AddItem(quest.RewardItem1, quest.RewardItem1Count);
            
            if(quest.RewardItem2 > 0)
                character.ItemManager.AddItem(quest.RewardItem2, quest.RewardItem2Count);
            
            if(quest.RewardItem3 > 0)
                character.ItemManager.AddItem(quest.RewardItem3, quest.RewardItem3Count);
            
            DBService.Instance.Save();
            return Result.Success;
        }
        sender.Session.Response.questSubmit.Errormsg = "任务不存在[数据库中没有]";
        return Result.Failed;
    }
    else
    {
        sender.Session.Response.questSubmit.Errormsg = "任务不存在[表中没有]";
        return Result.Failed;
    }
}
```

## 九、任务状态的刷新机制

客户端 `RefreshQuestStatus(quest)` 在接受或提交任务后刷新状态：

```csharp
private Quest RefreshQuestStatus(NQuestInfo quest)
{
    // 1. 清空 npcQuests 映射表
    this.npcQuests.Clear();
    
    // 2. 更新或添加任务
    Quest result;
    if(this.allQuests.ContainsKey(quest.QuestId))
    {
        // 更新已有任务的状态
        this.allQuests[quest.QuestId].info = quest;
        result = this.allQuests[quest.QuestId];
    }
    else
    {
        // 添加新任务
        result = new Quest(quest);
        this.allQuests[quest.QuestId] = result;
    }

    // 3. 检查新的可接受任务（前置任务解锁）
    CheckAvailableQuests();

    // 4. 重建 npcQuests 映射表
    foreach (var kv in this.allQuests)
    {
        this.AddNpcQuest(kv.Value.Define.AcceptNPC, kv.Value);
        this.AddNpcQuest(kv.Value.Define.SubmitNPC, kv.Value);
    }
    
    // 5. 触发任务状态变化事件
    if (OnQuestStatusChanged != null)
        OnQuestStatusChanged(result);
    
    return result;
}
```

刷新后，NPC 头上的任务标记会自动更新。

## 十、任务对话界面

`UIQuestDialog` 显示任务信息和操作按钮：

```csharp
public void SetQuest(Quest quest)
{
    this.quest = quest;
    this.UpdateQuest();
    
    if (this.quest.info == null)
    {
        // 未接受任务：显示接受按钮
        openButtons.SetActive(true);
        submitButtons.SetActive(false);
    }
    else
    {
        if(this.quest.info.Status == QuestStatus.Complated)
        {
            // 已完成任务：显示提交按钮
            openButtons.SetActive(false);
            submitButtons.SetActive(true);
        }
        else
        {
            // 进行中任务：不显示按钮
            openButtons.SetActive(false);
            submitButtons.SetActive(false);
        }
    }
}
```

按钮点击后触发 `OnClose` 事件：

- `WindowResult.Yes`：接受或提交任务
- `WindowResult.No`：拒绝任务，显示 DialogDeny



## 十一、还在制作中的功能 ... ...

【任务目标追踪】：击杀怪物和收集道具的进度更新机制。


## 十二、相关代码
1. `Lib/Common/Data/QuestDefine.cs`
2. `Server/GameServer/GameServer/TCharacterQuest.cs`
3. `Server/GameServer/GameServer/Managers/QuestManager.cs`
4. `Server/GameServer/GameServer/Services/QuestService.cs`
5. `Client/Assets/Scripts/Managers/QuestManager.cs`
6. `Client/Assets/Scripts/Services/QuestService.cs`
7. `Client/Assets/Scripts/Models/Quest.cs`
8. `Client/Assets/Scripts/UI/Quest/UIQuestDialog.cs`
9. `Client/Assets/Scripts/UI/Quest/UIQuestSystem.cs`
10. `Client/Assets/Scripts/UI/NpcQuestStatus.cs`
11. `Client/Assets/Scripts/Managers/NpcManager.cs`
