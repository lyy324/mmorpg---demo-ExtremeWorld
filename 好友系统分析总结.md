# 好友系统分析总结

## 一、好友系统的职责

好友系统主要解决五件事：

1. 记录玩家的好友关系，包括好友 ID、姓名、职业、等级等信息。
2. 处理好友添加请求，支持通过 ID 或姓名查找，双方确认后建立好友关系。
3. 处理好友删除请求，双向删除好友关系（从双方的好友列表中移除）。
4. 同步好友在线状态，好友上线或下线时通知相关玩家。
5. 提供好友列表界面，支持查看好友信息、添加好友、删除好友、邀请组队等操作。


## 二、整体实现方法

客户端与服务端的分工：

| `TCharacterFriend` ： 数据库实体，保存好友关系（CharacterID、FriendID、FriendName、Class、Level） |
| 服务端 `FriendManager` ： 每个角色一份，管理角色的好友列表，处理添加和删除操作 |
| 服务端 `FriendService` ： 处理好友请求和响应，转发消息，验证好友关系 |
| 客户端 `FriendManager` ： 管理当前玩家的好友列表数据 |
| 客户端 `FriendService` ： 发送好友请求，接收好友消息，处理响应 |
| `NFriendInfo` ： Protobuf 协议消息，传输好友信息（Id、friendInfo、Status） |
| `UIFriends` ： 好友界面，显示好友列表，提供添加、删除、组队邀请等功能 |
| `UIFriendItem` ： 好友列表项，显示好友姓名、职业、等级、在线状态 |


## 三、好友数据的定义

数据库实体 `TCharacterFriend` 保存好友关系：

| `Id` ： 数据库记录编号（好友关系的唯一标识） |
| `CharacterID` ： 角色 ID（拥有者） |
| `FriendID` ： 好友角色 ID |
| `FriendName` ： 好友姓名（用于离线显示） |
| `Class` ： 好友职业 （用于离线显示）|
| `Level` ： 好友等级 （用于离线显示）|
| `Owner` ： 关联的角色实体 (用于数据库通过角色查询好友信息) |

协议消息 `NFriendInfo` 传输好友信息：

| `Id` ： TCharacterFriend 的数据库记录 ID |
| `friendInfo` ： NCharacterInfo（好友的角色信息：Id、Name、Class、Level） |
| `Status` ： 在线状态（0 离线 / 1 在线） |

### 好友关系的双向存储

好友关系是双向的，A 添加 B 为好友后：
- A 的 `TCharacter.Friends` 中有一条记录：`CharacterID = A, FriendID = B`
- B 的 `TCharacter.Friends` 中有一条记录：`CharacterID = B, FriendID = A`

## 四、进入游戏时的好友初始化

--- 玩家进入游戏时的服务端流程
    Character 构造函数创建角色
    创建 FriendManager 实例
    调用 FriendManager.GetFriendInfos(Info.Friends)
    遍历 character.Data.Friends（数据库好友列表）
    为每个好友调用 GetFriendInfo(friend)
        检查好友是否在线（通过 CharacterManager）
        如果在线：获取实时角色信息，Status = 1，互相通知上线
        如果离线：使用数据库中的冗余数据，Status = 0
    写入 NCharacterInfo.Friends
    返回 UserGameEnterResponse


--- 玩家进入游戏时的客户端流程
    客户端收到进入游戏响应
    调用 FriendManager.Init(response.Character.Friends)
    保存好友列表到 allFriends
    如果打开了好友界面，触发 RefreshUI 刷新


## 五、好友在线状态的同步

服务端 `GetFriendInfo(TCharacterFriend friend)` 在初始化好友信息时处理在线状态：

```csharp
public NFriendInfo GetFriendInfo(TCharacterFriend friend)
{
    NFriendInfo friendInfo = new NFriendInfo();
    var character = CharacterManager.Instance.GetCharacter(friend.FriendID);
    friendInfo.friendInfo = new NCharacterInfo();
    friendInfo.Id = friend.Id;

    if (character == null)
    {
        // 好友离线 ： 使用数据库中的数据填充好友信息
        friendInfo.friendInfo.Id = friend.FriendID;
        friendInfo.friendInfo.Name = friend.FriendName;
        friendInfo.friendInfo.Class = (CharacterClass)friend.Class;
        friendInfo.friendInfo.Level = friend.Level;
        friendInfo.Status = 0;
    }
    else
    {
        // 好友在线：使用实时角色信息
        friendInfo.friendInfo = this.GetBasicInfo(character.Info);
        friendInfo.friendInfo.Name = character.Info.Name;
        friendInfo.friendInfo.Class = character.Info.Class;
        friendInfo.friendInfo.Level = character.Info.Level;
        
        // 同步等级变化到数据库
        if (friend.Level != character.Info.Level)
        {
            friend.Level = character.Info.Level;
        }
        
        // 通知好友：我上线了
        character.FriendManager.UpdateFriendInfo(this.Owner.Info, 1);
        friendInfo.Status = 1;
    }
    return friendInfo;
}
```

关键机制：

1. A 上线时，如果好友 B 也在线，A 的 `GetFriendInfo` 会调用 `B.FriendManager.UpdateFriendInfo(A, 1)`，通知 B："A 上线了"。
2. 检测到好友等级变化时，更新数据库中的冗余数据。
3. `UpdateFriendInfo` 设置 `friendChanged = true`，在 `PostProcess` 中重新初始化好友列表并发送 `FriendListResponse`（后处理机制）。

## 六、添加好友的流程

--- 添加好友的完整流程
    玩家 A 点击添加好友
    输入好友姓名或 ID
    
    客户端 FriendService.SendFriendAddRequest(friendId, friendName)
    发送 FriendAddRequest 消息
        FromId = A 的 ID
        FromName = A 的姓名
        ToId = 好友 ID（可为 0）
        ToName = 好友姓名
    
    服务端 FriendService.OnFriendAddRequest 接收请求
    如果 ToId == 0，使用 ToName 查找在线玩家
    验证：
        检查是否已经是好友
        检查好友是否存在且在线
    将请求转发给好友 B（friend.Session.Response.friendAddReq = request）
    
    好友 B 收到 FriendAddRequest 消息
    客户端 FriendService.OnFriendAddRequest 显示确认对话框
    玩家 B 选择接受或拒绝
    
    客户端 FriendService.SendFriendAddResponse(accept, request)
    发送 FriendAddResponse 消息（通过 Response 通道）
    
    服务端 FriendService.OnFriendAddResponse 接收响应
    如果接受（Result.Success）：
        获取请求者 A 的 Session
        A.FriendManager.AddFriend(B)
        B.FriendManager.AddFriend(A)
        保存数据库
        分别发送成功消息给 A 和 B
    如果拒绝：
        发送失败消息给 B
    
    客户端接收响应
    显示添加成功或失败的消息框


### 服务端添加好友的核心代码

```csharp
private void OnFriendAddResponse(NetConnection<NetSession> sender, FriendAddResponse response)
{
    Character character = sender.Session.Character;
    sender.Session.Response.friendAddRes = response;
    
    if (response.Result == Result.Success)
    {
        // 接受了好友请求
        var requester = SessionManager.Instance.GetSession(response.Request.FromId);
        if(requester == null)
        {
            sender.Session.Response.friendAddRes.Result = Result.Failed;
            sender.Session.Response.friendAddRes.Errormsg = "请求者下线了";
        }
        else
        {
            // 相互添加好友
            character.FriendManager.AddFriend(requester.Session.Character);
            requester.Session.Character.FriendManager.AddFriend(character);
            DBService.Instance.Save();
            
            // 发送消息给请求者 A
            requester.Session.Response.friendAddRes = response;
            requester.Session.Response.friendAddRes.Result = Result.Success;
            requester.Session.Response.friendAddRes.Errormsg = "添加好友成功";
            requester.SendResponse();
            
            // 发送消息给接受者 B
            sender.Session.Response.friendAddRes.Result = Result.Success;
            sender.Session.Response.friendAddRes.Errormsg = "添加好友 " + response.Request.FromName + "成功";
        }
    }
    sender.SendResponse();
}
```

`AddFriend` 方法创建 `TCharacterFriend` 实体并添加到数据库：

```csharp
public void AddFriend(Character friend)
{
    TCharacterFriend tf = new TCharacterFriend()
    {
        FriendID = friend.Id,
        FriendName = friend.Data.Name,
        Class = friend.Data.Class,
        Level = friend.Data.Level
    };
    this.Owner.Data.Friends.Add(tf);
    friendChanged = true; // 标记好友列表变化
}
```

## 七、删除好友的流程

--- 删除好友的完整流程
    玩家 A 选中好友 B
    点击删除好友按钮
    显示确认对话框
    
    客户端 FriendService.SendFriendRemoveRequest(Id, friendId)
    发送 FriendRemoveRequest 消息
        Id = TCharacterFriend 的数据库记录 ID
        friendId = 好友角色 ID
    
    服务端 FriendService.OnFriendRemove 接收请求
    调用 A.FriendManager.RemoveFriendById(Id)
        从 A.Data.Friends 中删除记录
        从数据库中删除 TCharacterFriend 实体
    检查好友 B 是否在线
        如果在线：B.FriendManager.RemoveFriendByFriendId(A.Id)
        如果离线：直接从数据库删除 B 的好友记录
    保存数据库
    返回 FriendRemoveResponse
    
    客户端接收响应
    显示删除成功或失败的消息框
    触发 FriendListResponse，刷新好友列表


### 服务端删除好友的核心代码

```csharp
private void OnFriendRemove(NetConnection<NetSession> sender, FriendRemoveRequest request)
{
    Character character = sender.Session.Character;
    sender.Session.Response.friendRemove = new FriendRemoveResponse();
    sender.Session.Response.friendRemove.Id = request.Id;

    // 删除自己的好友
    if (character.FriendManager.RemoveFriendById(request.Id))
    {
        sender.Session.Response.friendRemove.Result = Result.Success;
        
        // 删除别人好友中的自己
        var friend = SessionManager.Instance.GetSession(request.friendId);
        if (friend != null)
        {
            // 好友在线
            friend.Session.Character.FriendManager.RemoveFriendByFriendId(character.Id);
        }
        else
        {
            // 好友离线：直接从数据库删除
            this.RemoveFriend(request.friendId, character.Id);
        }
    }
    else
        sender.Session.Response.friendRemove.Result = Result.Failed;

    DBService.Instance.Save();
    sender.SendResponse();
}
```

## 八、好友列表的后处理机制

服务端 `FriendManager.PostProcess` 实现好友列表的自动同步：

```csharp
internal void PostProcess(NetMessageResponse message)
{
    if (this.friendChanged)
    {
        // 重新初始化好友列表
        this.InitFriends();
        
        // 附加 FriendListResponse 到响应消息
        if (message.friendList == null)
        {
            message.friendList = new FriendListResponse();
            message.friendList.Friends.AddRange(this.friends);
        }
        
        this.friendChanged = false;
    }
}
```

触发 `friendChanged` 的情况：

1. 添加好友（`AddFriend`）
2. 删除好友（`RemoveFriendById`、`RemoveFriendByFriendId`）
3. 好友上线或下线（`UpdateFriendInfo`）

客户端接收 `FriendListResponse` 后更新本地好友列表：

```csharp
private void OnFriendList(object sender, FriendListResponse message)
{
    FriendManager.Instance.allFriends = message.Friends;
    if (this.OnFriendUpdate != null)
        this.OnFriendUpdate(); // 触发 UI 刷新
}
```

## 九、好友界面的实现

`UIFriends` 提供好友列表和操作功能：

### 界面功能

| 好友列表 ： 显示所有好友，包括姓名、职业、等级、在线状态 |
| 添加好友 ： 输入姓名或 ID，发送好友请求 |
| 删除好友 ： 选中好友后删除 |
| 组队邀请 ： 邀请在线好友加入队伍 |
| 聊天功能 ： 聊天窗口变为私聊模式
 |
| 搜索好友 ： 根据姓名筛选好友列表 |

### 添加好友的输入验证

```csharp
private bool OnFriendAddSubmit(string input, out string tips)
{
    tips = "";
    int friendId = 0;
    string friendName = "";
    
    // 尝试解析为 ID，否则视为姓名
    if (!int.TryParse(input, out friendId))
    {
        friendName = input;
    }
    
    // 验证：不能添加自己为好友
    if (friendId == User.Instance.CurrentCharacter.Id || friendName == User.Instance.CurrentCharacter.Name)
    {
        tips = "不能添加自己为好友哦";
        return false;
    }
    
    FriendService.Instance.SendFriendAddRequest(friendId, friendName);
    return true;
}
```

### 删除好友的确认对话框

```csharp
public void OnClickFriendRemove()
{
    if (selectedItem == null)
    {
        MessageBox.Show("请选择要删除的好友");
        return;
    }
    
    MessageBox.Show(
        string.Format("确认要删除好友【{0}】吗", selectedItem.Info.friendInfo.Name), 
        "删除好友", 
        MessageBoxType.Confirm, 
        "删除", 
        "取消"
    ).OnYes = () => {
        FriendService.Instance.SendFriendRemoveRequest(
            this.selectedItem.Info.Id, 
            this.selectedItem.Info.friendInfo.Id
        );
    };
}
```

## 十、好友离线通知机制

当玩家下线时，通知所有在线好友：

```csharp
public void OfflineNotify()
{
    foreach (var friendinfo in friends)
    {
        var friend = CharacterManager.Instance.GetCharacter(friendinfo.friendInfo.Id);
        if (friend != null)
        {
            // 好友在线：通知好友我下线了
            friend.FriendManager.UpdateFriendInfo(this.Owner.Info, 0);
        }
    }
}
```

`UpdateFriendInfo(friendInfo, status)` 更新好友的在线状态：

```csharp
public void UpdateFriendInfo(NCharacterInfo friendInfo, int status)
{
    foreach (var friend in this.friends)
    {
        if (friend.friendInfo.Id == friendInfo.Id)
        {
            friend.Status = status; // 0 离线 / 1 在线
            break;
        }
    }
    this.friendChanged = true; // 触发后处理机制
}
```

## 十一、相关代码

1. `Server/GameServer/GameServer/TCharacterFriend.cs`
2. `Server/GameServer/GameServer/Managers/FriendManager.cs`
3. `Server/GameServer/GameServer/Services/FriendService.cs`
4. `Client/Assets/Scripts/Managers/FriendManager.cs`
5. `Client/Assets/Scripts/Services/FriendService.cs`
6. `Client/Assets/Scripts/UI/Friend/UIFriends.cs`
7. `Client/Assets/Scripts/UI/Friend/UIFriendItem.cs`
8. `Server/GameServer/GameServer/Entities/Character.cs`
9. `Lib/Protocol/message.cs` (NFriendInfo, FriendAddRequest/Response, FriendRemoveRequest/Response)
10. `Server/GameServer/GameServer/Managers/SessionManager.cs`
11. `Server/GameServer/GameServer/Managers/CharacterManager.cs`
