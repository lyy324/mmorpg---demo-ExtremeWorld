# 道具系统分析总结

## 一、道具系统的职责

道具系统主要解决四件事：

1. 定义一种道具的固定信息，例如名称、图标、类型、堆叠上限和使用效果。
2. 记录某个角色拥有这种道具的总数量。
3. 在获得、扣除、使用道具时修改服务端数据并保存。
4. 将数量变化同步给客户端，让本地缓存和界面保持正确。


## 二、整体实现方法

客户端与服务端的分工：

| `ItemDefine` ： 道具的定义，定义了道具的 ID，名称、图标、类型、堆叠上限和使用效果 |
| 服务端 `ItemManager` ： 每个角色一份，管理角色的道具数据，如增加道具、移除道具等 |
| 服务端 `Models.Item` ： 将数据库中的数据转为内存中的对象，并封装了增加和减少道具数量的方法 |
| `TCharacterItem` ： 数据库中保存的角色的道具信息 |
| 客户端 `ItemManager` ： 用字典存储服务器返回的道具数据，按道具 ID 分类，封装了增加和减少道具数量的方法  |
| `ItemService` ： 负责请求和响应的网络收发 |
| `StatusManager` ： 服务端收集道具增减事件 |
| `StatusService` ： 客户端接收并分发增减事件 |

服务端 `ItemManager` 是每个 `Character` 一份，避免不同玩家共用数据；客户端只管理当前登录角色。
客户端 `ItemManager` 则不用，客户端只有一个角色，直接放到`ItemManager`的字典里。

## 三、道具数据的定义

`Lib/Common/Data/ItemDefine.cs` 中的 `ItemDefine` 描述“这种道具是什么”，不记录玩家拥有数量。

| `ID` ： 道具 ID |
| `Name`、`Description` ： 名称和描述 |
| `Type`、`Category` ： 类型和分类 |
| `Level`、`LimitClass` ： 等级和职业限制 |
| `CanUse`、`UseCD` ： 是否可用、使用冷却 |
| `StackLimit` ： 单组最大数量 |
| `Icon` ： 图标资源路径 |
| `Function`、`Param`、`Params` ： 使用时执行的功能和参数 |

数据通过 Excel 表格定义，导出 JSON 文件，再复制到客户端和服务端运行目录：

--- 导表流程
Data/Tables/ItemDefine.xlsx
    Data/Excel2Json.cmd 导出 JSON
    Data/Data/ItemDefine.txt
    复制到客户端和服务端运行目录
    两端 DataManager 反序列化为 Dictionary<int, ItemDefine>


配置里的 `Function = RecoverHP`、`Param = 500` 前者是使用时执行的功能，后者是参数值，还在制作中... ...

## 四、存在内存以及数据库中的道具数据

数据库实体 `TCharacterItem` 保存一条角色道具记录：

--- TCharacterItem
Id          数据库记录编号
CharacterID 所属角色
ItemID      道具配置 ID
ItemCount   角色拥有的总数量


服务端 `GameServer.Models.Item` 包装这个实体，并在内存中保存 `ItemID` 和 `Count`。由 `ItemManager.Items` 按道具 ID 建立字典：

--- ItemManager.Items
Items[1] = 小型治疗药水，总数量 120
Items[2] = 小型法力药水，总数量 20


增加或扣除时，模型同时改内存值和数据库实体：

--- Item.Add
Item.Add(count)
    Count += count
    dbItem.ItemCount = Count
    DBService.Save()


## 五、进入游戏的流程

--- 玩家进入游戏时的服务端流程
    玩家进入游戏
    Character 创建自己的 ItemManager
    遍历 character.Data.Items
    TCharacterItem 包装成 Models.Item
    放入 Dictionary<int, Item>
    GetItemInfos 转成 NItemInfo(Id, Count)
    写入 NCharacterInfo.Items
    返回 UserGameEnterResponse

`NItemInfo` 是在 Protobuf 中定义的协议，用于传输道具 ID 和数量。

--- 玩家进入游戏时的客户端流程
    客户端收到进入游戏响应后调用 `ItemManager.Init(response.Character.Items)`：
    清空旧字典。
    为每个 `NItemInfo` 创建客户端 `Models.Item`。
    用 ID 查询本地 `ItemDefine`，补全名称、图标和规则。
    注册 `StatusService` 的道具通知回调。

## 六、游戏中的道具变化先通知服务端，服务端数据变化

``` AddItem
    public bool AddItem(int itemID,int count)
    {
        Item item = null;

        if(this.Items.TryGetValue(itemID, out item))
        {
            item.Add(count);
        }
        else
        {
            TCharacterItem dbItem = new TCharacterItem();
            dbItem.CharacterID = Owner.Data.ID;
            dbItem.Owner = Owner.Data;
            dbItem.ItemID = itemID;
            dbItem.ItemCount = count;
            Owner.Data.Items.Add(dbItem);
            this.Items.Add(itemID,new Item(dbItem));
        }
        this.Owner.StatusManager.AddItemChange(itemID, count, StatusAction.Add);
        Log.InfoFormat("AddItem: characterID:{0}:{1} ItemID:{2} Count:{3}", Owner.Id, Owner.Data, itemID, count);
        DBService.Instance.Save();
        return true;
    }
```

当前 `AddItem()` 的实现步骤：

1. 在 `Items` 字典中查找道具。
2. 如果存在，增加 `Item.Count`，并同步数据库实体的 `ItemCount`。
3. 如果不存在，创建新的 `TCharacterItem`，关联角色、道具 ID 和数量，再加入字典。
4. 调用 `StatusManager.AddItemChange(itemId, count, StatusAction.Add)` 记录增量。
5. 调用 `DBService.Save()` 保存。


``` RemoveItem
    public bool RemoveItem(int itemID,int count)
    {
        if(!this.Items.ContainsKey(itemID))
            return false;
        Item item = this.Items[itemID];
        if (item.Count < count)
            return false;
        item.Remove(count);
        Log.InfoFormat("[{0}] RemoveItem[{1}] removeCount: {2}",this.Owner.Data.ID,item,count);
        DBService.Instance.Save();
        return true;
    }
```
在商店购买道具，以及在任务奖励获得道具时，先把消息发给服务端，服务端再根据道具 ID 和数量，调用 `AddItem()` 或 `RemoveItem()` 方法。


## 七、用统一的状态通知把变化同步给客户端

购买和任务奖励不直接修改客户端，而是走统一的状态通知。

增加道具时产生一条 `NStatus`：
--- NStatus
Type   = StatusType.Item
Action = StatusAction.Add
Id     = 道具 ID
Value  = 本次增加数量（为负值表示扣除）

采用后处理机制修改道具状态，如果状态变化，才附加到消息中，随其他消息一起发给客户端。

--- NetSession.GetResponse()
    Character.PostProcess()
    StatusManager.PostProcess()
    将状态放入 response.statusNotify
    清空状态队列
    打包发送响应

客户端接收并应用道具状态通知

--- StatusService.OnStatusNotify
    遍历 NStatus
    Type == Item
    ItemManager.OnItemNotify
    Add：增加本地数量
    Delete：减少本地数量


## 八、任务奖励获得道具的流程

--- 任务奖励获得道具的流程
    QuestManager.SubmitRequest
    检查任务完成条件
    character.ItemManager.AddItem(rewardItemId, rewardCount)
    更新内存和数据库实体
    记录 Item 增加状态
    保存数据库
    任务响应发送前附加 StatusNotify
    客户端 StatusService 收到通知
    ItemManager 更新本地数量

任务系统只负责决定奖励，道具系统负责发放、保存和同步。

##  九、还在制作中的功能 ... ...
【消耗品使用】


## 十、相关代码

1. `Lib/Common/Data/ItemDefine.cs`
2. `Server/GameServer/GameServer/TCharacterItem.cs`
3. `Server/GameServer/GameServer/Models/Item.cs`
4. `Server/GameServer/GameServer/Managers/ItemManager.cs`
5. `Server/GameServer/GameServer/Entities/Character.cs`
6. `Server/GameServer/GameServer/Managers/StatusManager.cs`
7. `Server/GameServer/GameServer/Network/NetSession.cs`
8. `Client/Assets/Scripts/Services/StatusService.cs`
9. `Client/Assets/Scripts/Managers/ItemManager.cs`
10. `Client/Assets/Scripts/Models/Item.cs`


