# 背包系统分析总结

## 一、背包系统的职责

背包系统主要解决四件事：

1. 提供固定容量的格子存储道具，每个格子记录道具 ID 和数量。
2. 处理道具的堆叠规则，根据 StackLimit 自动拆分或合并道具。
3. 将背包数据以二进制字节数组的形式存储到数据库，节省存储空间。
4. 在客户端展示背包界面，显示道具图标、数量，支持分页和重置。


## 二、整体实现方法

背包系统客户端与服务端的分工：

| `BagItem` ： 背包格子的结构体，包含道具 ID 和数量，使用 StructLayout 紧凑排列 |
| `TCharacterBag` ： 数据库实体，保存背包的二进制数据（Items）和解锁格子数（Unlocked） |
| 客户端 `BagManager` ： 管理背包数据，负责初始化、增加道具、移除道具、序列化和反序列化 |
| 服务端 `BagService` ： 处理背包保存请求，将客户端传来的背包数据保存到数据库 |
| `UIBag` ： 背包界面，展示背包格子、道具图标和数量，支持分页和重置功能 |
| `NBagInfo` ： Protobuf 协议消息，传输背包的解锁格子数和二进制道具数据 |


## 三、背包数据的定义

`Models.BagItem` 是背包格子的核心结构体：

```csharp
[StructLayout(LayoutKind.Sequential, Pack = 1)]
struct BagItem 
{
    public ushort ItemId;  // 道具 ID（0 表示空格子）
    public ushort Count;   // 道具数量
}
```
使用结构体的原因是因为结构体时值类型，一个物品占用 4 字节，传递消息时一个物品只需传四字节，比传统的方法更高效。

关键特性：

| `StructLayout(LayoutKind.Sequential, Pack = 1)` ： 让结构体在内存中紧凑排列，无填充字节 |
| `ushort` ： 使用 16 位无符号整数，范围 0-65535，每个 BagItem 占 4 字节 |
| `ItemId = 0` ： 表示空格子 |
| `sizeof(BagItem) = 4` ： 每个格子固定占用 4 字节 |

数据库实体 `TCharacterBag` 保存背包数据：

| `Id` ： 数据库记录编号 |
| `Items` ： 二进制字节数组，存储所有格子的 BagItem 数据 |
| `Unlocked` ： 已解锁的格子数量 |
| `Owner` ： 关联的角色实体 |


## 四、进入游戏时的背包初始化

--- 玩家进入游戏时的服务端流程
    Character 构造函数创建角色
    读取 character.Data.Bag（TCharacterBag 实体）
    创建 NBagInfo 协议消息
    设置 Unlocked = Data.Bag.Unlocked
    设置 Items = Data.Bag.Items（字节数组）
    写入 NCharacterInfo.Bag
    返回 UserGameEnterResponse

服务端直接将数据库中的二进制数据传递给客户端，无需反序列化。

--- 玩家进入游戏时的客户端流程
    客户端收到进入游戏响应
    调用 BagManager.Init(response.Character.Bag)
    保存 NBagInfo 引用
    设置 Unlocked 解锁格子数
    创建 BagItem[] Items 数组
    调用 Analyze(info.Items) 反序列化二进制数据
    使用 unsafe 指针操作将字节数组转换为 BagItem 数组


## 五、背包数据的序列化和反序列化

背包使用 unsafe 指针操作 实现高效的二进制序列化。

反序列化（字节数组 → BagItem 数组）

```csharp
unsafe void Analyze(byte[] data)
{
    fixed (byte* pt = data)
    {
        for(int i = 0; i < this.Unlocked; i++)
        {
            BagItem* item = (BagItem*)(pt + i * sizeof(BagItem));
            Items[i] = *item;
        }
    }
}
```

流程说明：

1. 使用 `fixed` 关键字固定 `data` 数组的内存地址，防止垃圾回收器移动。
2. 计算每个格子的偏移量：`pt + i * sizeof(BagItem)`。
3. 将指针指向的 4 字节数据解释为 `BagItem` 结构体。
4. 复制到 `Items[i]`。

序列化（BagItem 数组 → 字节数组）

```csharp
unsafe public NBagInfo GetBagInfo()
{
    fixed (byte* pt = Info.Items)
    {
        for(int i = 0; i < this.Unlocked; i++)
        {
            BagItem* item = (BagItem*)(pt + i * sizeof(BagItem));
            *item = Items[i];
        }
    }
    return this.Info;
}
```

流程说明：

1. 固定 `Info.Items` 字节数组的内存地址。
2. 将 `Items[i]` 的数据写入到对应的字节位置。
3. 返回 `NBagInfo` 用于网络传输或数据库保存。

## 六、背包与道具系统的协作

背包系统的 `Reset()` 方法从道具系统重建背包布局：

```csharp
public void Reset()
{
    int i = 0;
    foreach(var kv in ItemManager.Instance.Items)
    {
        if(kv.Value.Count <= kv.Value.Define.StackLimit)
        {
            // 单组就能放下
            this.Items[i].ItemId = (ushort)kv.Key;
            this.Items[i].Count = (ushort)kv.Value.Count;
        }
        else
        {
            // 超过堆叠上限，拆分到多个格子
            int count = kv.Value.Count;
            while(count > kv.Value.Define.StackLimit)
            {
                this.Items[i].ItemId = (ushort)kv.Key;
                this.Items[i].Count = (ushort)kv.Value.Define.StackLimit;
                i++;
                count -= kv.Value.Define.StackLimit;
            }
            if(count != 0)
            {
                this.Items[i].ItemId = (ushort)kv.Key;
                this.Items[i].Count = (ushort)count;
            }
            else
            {
                i--;
            }
        }
        i++;
    }
}
```

`Reset()` 的作用：

1. 遍历道具系统的 `ItemManager.Instance.Items`（道具总量）。
2. 根据 `StackLimit`（堆叠上限）将道具分配到背包格子。
3. 如果道具数量超过堆叠上限，自动拆分到多个格子。
4. 重建背包的格子排列。

## 七、向背包添加道具的流程

`BagManager.AddItem(itemId, count)` 实现了 全有或全无 的添加策略：

```csharp
public bool AddItem(int itemId, int count)
{
    // 1. 参数验证
    if (this.Items == null || count <= 0 || DataManager.Instance.Items == null)
        return false;

    // 2. 获取道具定义
    Common.Data.ItemDefine define;
    if (!DataManager.Instance.Items.TryGetValue(itemId, out define))
        return false;

    int stackLimit = define.StackLimit;
    if (stackLimit <= 0 || stackLimit > ushort.MaxValue || itemId <= 0 || itemId > ushort.MaxValue)
        return false;

    // 3. 预估容量（核心：全有或全无）
    long capacity = 0;
    for (int i = 0; i < this.Items.Length; i++)
    {
        if (this.Items[i].ItemId == itemId)
        {
            int free = stackLimit - this.Items[i].Count;
            if (free > 0)
                capacity += free;
        }
        else if (this.Items[i].ItemId == 0)
        {
            capacity += stackLimit;
        }
    }

    if (capacity < count)
        return false; // 容量不足，拒绝添加

    // 4. 第一阶段：优先填充已有的同类堆
    int remaining = count;
    for (int i = 0; i < this.Items.Length && remaining > 0; i++)
    {
        if (this.Items[i].ItemId != itemId)
            continue;

        int free = stackLimit - this.Items[i].Count;
        if (free <= 0)
            continue;

        int add = Math.Min(remaining, free);
        this.Items[i].Count = (ushort)(this.Items[i].Count + add);
        remaining -= add;
    }

    // 5. 第二阶段：拆分到空格子
    for (int i = 0; i < this.Items.Length && remaining > 0; i++)
    {
        if (this.Items[i].ItemId != 0)
            continue;

        int add = Math.Min(remaining, stackLimit);
        this.Items[i].ItemId = (ushort)itemId;
        this.Items[i].Count = (ushort)add;
        remaining -= add;
    }

    return remaining == 0;
}
```

添加物品到背包的流程：

1. 先计算背包能否完全容纳 count 个道具。
2. 如果容量不足，拒绝添加，避免部分写入导致数据不一致。
3. 先填充已有的同类道具堆，减少占用的格子数。
4. 剩余数量按 StackLimit 拆分到空格子。

## 八、背包界面的展示

`UIBag` 负责背包界面内容的显示：

--- UIBag 的初始化流程
    Start() 启动时调用 InitBags()
    遍历 BagManager.Instance.Items 数组
    如果格子的 ItemId > 0
        实例化 bagItem 预制件
        从 ItemManager 获取道具定义
        设置道具图标和数量
    如果格子索引超过 Unlocked
        将格子颜色设为灰色（未解锁状态）


关键代码：

```csharp
IEnumerator InitBags()
{
    for(int i = 0; i < BagManager.Instance.Items.Length; i++)
    {
        var item = BagManager.Instance.Items[i];
        if(item.ItemId > 0)
        {
            GameObject go = Instantiate(bagItem, slots[i].transform);
            var ui = go.GetComponent<UIIconItem>();
            var def = ItemManager.Instance.Items[item.ItemId].Define;
            ui.SetMainIcon(def.Icon, item.Count.ToString());
        }
        yield return null;
    }
    for (int i = BagManager.Instance.Items.Length; i < slots.Count; i++)
    {
        slots[i].color = Color.gray; // 未解锁格子显示为灰色
        yield return null;
    }
}
```

界面功能：

| 显示玩家金币 ： `User.Instance.CurrentCharacter.Gold` |
| 分页显示 ： `pages[]` 数组支持多页背包 |
| 道具图标 ： 从 `ItemDefine.Icon` 加载 |
| 道具数量 ： 显示 `BagItem.Count` |
| 重置按钮 ： 调用 `BagManager.Reset()` 重建背包布局 |

## 九、背包数据的保存

--- 背包保存流程
    客户端修改背包数据（拖拽、整理等）
    调用 BagManager.GetBagInfo() 序列化背包数据
    发送 BagSaveRequest 消息
        包含 NBagInfo（Unlocked 和 Items 字节数组）
    
    服务端 BagService.OnBagSave 接收请求
    获取 Character 实体
    将 request.BagInfo.Items 写入 character.Data.Bag.Items
    调用 DBService.Instance.Save() 保存到数据库


服务端代码：

```csharp
void OnBagSave(NetConnection<NetSession> sender, BagSaveRequest request)
{
    Character character = sender.Session.Character;
    Log.InfoFormat("BagSaveRequest: character: {0}: Unlocked{1}", character.Id, request.BagInfo.Unlocked);

    if(request.BagInfo != null)
    {
        character.Data.Bag.Items = request.BagInfo.Items;
        DBService.Instance.Save();
    }
}
```


## 十、还在制作中的功能 ... ...

【移除道具】：`RemoveItem(itemId, count)` 方法尚未实现。
【道具拖拽】：拖拽道具改变格子位置的交互功能。


## 十三、相关代码

1. `Client/Assets/Scripts/Models/BagItem.cs`
2. `Client/Assets/Scripts/Managers/BagManager.cs`
3. `Client/Assets/Scripts/UI/UIBag.cs`
4. `Server/GameServer/GameServer/TCharacterBag.cs`
5. `Server/GameServer/GameServer/Services/BagService.cs`
6. `Server/GameServer/GameServer/Entities/Character.cs`
7. `Lib/Protocol/message.cs` 
8. `Client/Assets/Scripts/Managers/ItemManager.cs`
9. `Lib/Common/Data/ItemDefine.cs`
