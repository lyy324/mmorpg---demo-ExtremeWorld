# 商店系统分析总结

## 一、商店系统的职责

商店系统主要解决四件事：

1. 定义商店的基本信息，例如商店名称、描述和商品列表。
2. 定义商店中每个商品的道具 ID、数量、价格和状态。
3. 处理玩家购买商品的请求，验证金币是否足够，扣除金币并发放道具。
4. 通过 NPC 交互触发商店界面的打开，展示商品列表并支持分页浏览。


## 二、整体实现方法

客户端与服务端的分工：

| `ShopDefine` ： 商店的定义，包含商店 ID、名称、描述和状态 |
| `ShopItemDefine` ： 商店商品的定义，包含道具 ID、数量、价格和状态 |
| 服务端 `ShopManager` ： 单例管理器，处理购买请求，验证价格和库存，扣除金币并调用 `ItemManager.AddItem` 发放道具 |
| 客户端 `ShopManager` ： 负责打开商店界面，注册 NPC 交互事件，发送购买请求 |
| `ItemService` ： 服务端接收 `ItemBuyRequest` 消息，调用 `ShopManager.BuyItem` 处理购买逻辑，返回 `ItemBuyResponse` |
| 客户端 `ItemService` ： 发送 `SendBuyItem` 请求到服务端 |
| `UIShop` ： 商店界面，展示商店名称、玩家金币、商品列表，支持分页和购买操作 |
| `UIShopItem` ： 商品单项 UI，显示道具图标、名称、价格、数量和职业限制 |

商店系统依赖道具系统和金币系统，购买成功后通过道具系统的统一通知机制同步道具变化。

## 三、商店和商品数据的定义

`Lib/Common/Data/ShopDefine.cs` 中的 `ShopDefine` 定义这是什么类型的商店：

| `ID` ： 商店 ID |
| `Name` ： 商店名称 |
| `Description` ： 商店描述 |
| `Status` ： 商店状态 |

`Lib/Common/Data/ShopItemDefine.cs` 中的 `ShopItemDefine` 定义商店中的商品信息：

| `ItemID` ： 道具 ID（关联 ItemDefine） |
| `Count` ： 购买时获得的道具数量 |
| `Price` ： 商品价格（金币） |
| `Status` ： 商品状态（大于 0 表示可用） |

数据通过 Excel 表格定义，导出 JSON 文件，再复制到客户端和服务端运行目录：

--- 导表流程
Data/Tables/ShopDefine.xlsx
Data/Tables/ShopItemDefine.xlsx
    Data/Excel2Json.cmd 导出 JSON
    Data/Data/ShopDefine.txt
    Data/Data/ShopItemDefine.txt
    复制到客户端和服务端运行目录
    两端 DataManager 反序列化为 Dictionary


服务端 `DataManager.Shops` 存储 `Dictionary<int, ShopDefine>`。
服务端 `DataManager.ShopItems` 存储 `Dictionary<int, Dictionary<int, ShopItemDefine>>`，第一层 key 是商店 ID，第二层 key 是商品 ID。

## 四、打开商店的流程

--- 玩家通过 NPC 打开商店的流程
    玩家与 NPC 交互
    如果NPC 的 Function 为 InvokeShop
    客户端 NpcManager 触发 NpcFunction.InvokeShop 事件
    ShopManager.OnOpenShop 接收 npc.Param（商店 ID）
    ShopManager.ShowShop(shopId)
    查询 DataManager.Instance.Shops
    UIManager 显示 UIShop 界面
    UIShop.SetShop(shop) 设置商店信息
    InitItems 初始化商品列表
    RefreshPage 刷新当前页商品


客户端 `ShopManager.Init()` 在初始化时注册 NPC 交互事件：

```csharp
NpcManager.Instance.RegisterNpcEvent(Common.Data.NpcFunction.InvokeShop, OnOpenShop);
```

## 五、商店界面的展示

`UIShop` 负责商店界面的展示和交互：

1. 商店信息：显示商店名称（title）和玩家当前金币（money）。
2. 商品列表：从 `DataManager.Instance.ShopItems[shopId]` 获取商品，过滤 `Status > 0` 的可用商品。
3. 分页功能：每页显示 8 个商品（`ItemsPerPage = 8`），支持上一页和下一页按钮。
4. 商品选择：玩家点击商品后，`UIShopItem.OnSelect` 触发，背景变为选中状态。
5. 购买按钮：点击购买按钮后，`UIShop.OnClickBuy` 调用 `ShopManager.BuyItem`。

`UIShopItem` 显示单个商品的信息（传入 道具id ShopItemDefine 和 UIShop 实现初始化）：

| `icon` ： 道具图标（从 ItemDefine.Icon 加载） |
| `title` ： 道具名称（从 ItemDefine.Name 获取） |
| `price` ： 商品价格（从 ShopItemDefine.Price 获取） |
| `count` ： 购买数量（从 ShopItemDefine.Count 获取） |
| `limitClass` ： 职业限制（从 ItemDefine.LimitClass 获取） |

## 六、购买商品的流程

--- 购买商品的完整流程
    客户端 UIShop.OnClickBuy 点击购买按钮
    检查是否选中商品
    客户端 ShopManager.BuyItem(shopId, shopItemId)
    客户端 ItemService.SendBuyItem(shopId, shopItemId)
    发送 ItemBuyRequest 消息到服务端
    
    服务端 ItemService.OnItemBuy 接收请求
    调用 ShopManager.Instance.BuyItem(sender, shopId, shopItemId)
    验证商店和商品是否存在
    检查玩家金币是否足够
    扣除金币：character.Gold -= shopItem.Price
    调用 ItemManager.AddItem(shopItem.ItemID, shopItem.Count)
    保存数据库 DBService.Instance.Save()
    返回 ItemBuyResponse 结果
    
    道具系统的状态通知机制自动同步道具变化
    客户端更新背包（使用委托 `ItemManager.OnItemNotify` 处理道具增加通知）


## 七、购买商品时的验证和处理

服务端 `ShopManager.BuyItem()` 的实现步骤：

```csharp
public Result BuyItem(NetConnection<NetSession> sender, int shopId, int shopItemId)
{
    // 1. 验证商店是否存在
    if (!DataManager.Instance.Shops.ContainsKey(shopId))
        return Result.Failed;
    
    // 2. 验证商品是否存在
    ShopItemDefine shopItem;
    if (DataManager.Instance.ShopItems[shopId].TryGetValue(shopItemId, out shopItem))
    {
        // 3. 验证金币是否足够
        if(sender.Session.Character.Gold >= shopItem.Price)
        {
            // 4. 发放道具
            sender.Session.Character.ItemManager.AddItem(shopItem.ItemID, shopItem.Count);
            
            // 5. 扣除金币
            sender.Session.Character.Gold -= shopItem.Price;
            
            // 6. 保存数据库
            DBService.Instance.Save();
            
            return Result.Success;
        }
    }
    return Result.Failed;
}
```

购买成功后，`ItemManager.AddItem` 会自动触发道具增加的状态通知，客户端通过 `StatusService` 接收并更新本地数据。

## 八、金币变化的同步

商店购买会扣除玩家金币，金币变化通过以下方式同步：

1. 服务端直接修改 `Character.Gold` 属性。
2. `Character.Gold` 的变化会自动触发金币变化通知（依旧是后处理机制）。
3. 客户端定义的事件 `User.Instance.GoldChange` 被触发。
4. `UIShop.UpDateMoney` 等订阅了事件的客户端会自动更新界面显示的金币数量。

```csharp
void UpDateMoney(int money)
{
    this.money.text = money.ToString();
}
```
## 九、相关代码

1. `Lib/Common/Data/ShopDefine.cs`
2. `Lib/Common/Data/ShopItemDefine.cs`
3. `Server/GameServer/GameServer/Managers/ShopManager.cs`
4. `Server/GameServer/GameServer/Services/ItemService.cs`
5. `Client/Assets/Scripts/Managers/ShopManager.cs`
6. `Client/Assets/Scripts/Services/ItemService.cs`
7. `Client/Assets/Scripts/UI/Shop/UIShop.cs`
8. `Client/Assets/Scripts/UI/Shop/UIShopItem.cs`
9. `Client/Assets/Scripts/Managers/NpcManager.cs`
10. `Client/Assets/Scripts/Models/User.cs`
